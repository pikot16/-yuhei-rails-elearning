<% if object.errors.any? %> <%# @userをobjectのままにするとユーザー編集ページアクセス時にエラーになる %>
    <div class="alert alert-danger">
        <% object.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
    </div>
<% end %>